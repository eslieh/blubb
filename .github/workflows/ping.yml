name: Ping Render App

on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your site
        run: curl -s https://blubb.onrender.com/health
